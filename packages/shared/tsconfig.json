{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		// 共享包可开启编译输出（可选）
		"noEmit": false,
		"outDir": "./dist",
		"rootDir": "./src"
	},
	"include": ["src/**/*"],
	"exclude": ["node_modules", "dist"]
}
