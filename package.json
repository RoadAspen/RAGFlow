{
	"name": "RAGFlow",
	"version": "1.0.0",
	"description": "",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:package": "pnpm -r --filter \"./packages/*\" run build",
		"dev:web": "pnpm run dev:package && pnpm -r --filter \"web\" run start:dev",
		"dev:server": "pnpm run dev:package && pnpm --filter \"server\" run start:dev",
		"dev:all": "pnpm run dev:package && pnpm -r --parallel --if-present --filter \"./packages/*\" --filter \"./apps/*\" run start:dev",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.33.0",
	"devDependencies": {}
}
